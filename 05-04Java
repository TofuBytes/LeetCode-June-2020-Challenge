class Solution {
    public void reverseString(char[] s) {      
        for(int x = 0; x < s.length/2; x++){
            int pos = (s.length-1)-x;
            char last = s[pos];  
            char first = s[x];
            s[x] = last;
            s[pos] = first; 
        }       
    }
}
